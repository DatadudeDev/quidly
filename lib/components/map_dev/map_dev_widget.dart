import '/flutter_flow/flutter_flow_theme.dart';
import '/flutter_flow/flutter_flow_util.dart';
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';
import 'package:provider/provider.dart';
import 'map_dev_model.dart';
export 'map_dev_model.dart';

class MapDevWidget extends StatefulWidget {
  const MapDevWidget({Key? key}) : super(key: key);

  @override
  _MapDevWidgetState createState() => _MapDevWidgetState();
}

class _MapDevWidgetState extends State<MapDevWidget> {
  late MapDevModel _model;

  @override
  void setState(VoidCallback callback) {
    super.setState(callback);
    _model.onUpdate();
  }

  @override
  void initState() {
    super.initState();
    _model = createModel(context, () => MapDevModel());

    WidgetsBinding.instance.addPostFrameCallback((_) => setState(() {}));
  }

  @override
  void dispose() {
    _model.maybeDispose();

    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
